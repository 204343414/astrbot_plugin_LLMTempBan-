{
  "administrators": {
    "description": "管理员用户ID列表（支持多个,BOT账号会自动添加为管理员）",
    "type": "list",
    "hint": "输入用户ID（字符串格式），多个ID用英文逗号分隔或分行。例如：['123456', '789012']",
    "default": []
  },
  "default_blacklist_duration": {
    "description": "默认拉黑时长（分钟）",
    "type": "int",
    "hint": "未指定时长时，拉黑操作的默认持续时间（必须为正整数）",
    "default": 5
  },
  "ignore_cooldown": {
    "description": "已读不回冷却时间（秒），冷却期内直接跳过LLM不烧token",
    "type": "int",
    "hint": "在已读不回后，这段时间内该会话的消息直接被跳过，不调用LLM，防止token浪费",
    "default": 120
  },
  "enable_auto_spam_blacklist": {
    "description": "启用自动刷屏/图片spam检测拉黑",
    "type": "bool",
    "default": true,
    "hint": "检测到同一用户在x秒内连续y次主动触发bot消息时，自动临时拉黑z分钟。同时在on_llm_request中尽早stop_event避免烧token。参考recall_cancel插件的hook使用风格。"
  },
  "spam_window_seconds": {
    "description": "自动拉黑检测窗口（秒）",
    "type": "int",
    "hint": "在此时间窗口内统计连续主动发消息次数",
    "default": 60
  },
  "spam_threshold": {
    "description": "自动拉黑阈值（次数）",
    "type": "int",
    "hint": "窗口内达到此连续触发次数即自动拉黑（建议4-8）",
    "default": 5
  },
  "auto_blacklist_duration_minutes": {
    "description": "自动拉黑时长（分钟）",
    "type": "int",
    "hint": "检测到刷屏后自动拉黑的默认时长",
    "default": 10
  },
  "permanent_ban_messages": {
    "description": "永久拉黑用户的自动回复语录列表（支持模板变量）",
    "type": "list",
    "hint": "被永久拉黑的用户再次触发 Bot 时，会每隔 'permanent_ban_reply_interval' 秒随机抽取一条发送。可用模板变量：{user_id} 用户ID、{duration} 拉黑时长（永久时显示'永久'）、{ban_time} 拉黑时间（如 2026-06-25 14:30）。",
    "default": [
      "您已被拉黑 {user_id}，已拉黑 {duration}。",
      "被拉黑还锲而不舍地戳 Bot，建议输入 /删除bot 或自行删除 Bot 好友，对大家都好~",
      "您已被永久拉黑，请继续表演，反正 Bot 不会再理你了。",
      "黑名单里的空气还好吗？{user_id} 同学。",
      "低质量骚扰已触发永久屏蔽，您已收获 Bot 的沉默大礼包。"
    ]
  },
  "friend_permanent_ban_messages": {
    "description": "【可选】好友专用永久拉黑语录列表（为空则使用通用语录）",
    "type": "list",
    "hint": "如果目标用户是 Bot 的 QQ 好友，且被永久拉黑，会优先使用这里的语录；为空则使用上面的通用语录。支持同样的模板变量。",
    "default": []
  },
  "permanent_ban_reply_interval": {
    "description": "永久拉黑自动回复间隔（秒）",
    "type": "int",
    "hint": "被永久拉黑的用户每次触发 Bot 后，至少要间隔这么多秒才会再次收到自动回复语录。默认 3600 秒 = 1 小时。",
    "default": 3600
  },
  "auto_delete_friend_on_permanent_ban": {
    "description": "永久拉黑好友时是否自动删除好友（参考 HappyBirthday 插件的好友列表检测）",
    "type": "bool",
    "hint": "开启后，当管理员/LLM 永久拉黑的目标用户是 Bot 的 QQ 好友时，会自动调用 OneBot 接口删除好友。默认关闭，请确认你的协议端支持 delete_friend 再开启。",
    "default": false
  },
  "friend_list_refresh_interval": {
    "description": "好友列表缓存刷新间隔（秒）",
    "type": "int",
    "hint": "检测用户是否为 Bot 好友时，会缓存好友列表并按此间隔刷新。默认 3600 秒 = 1 小时。",
    "default": 3600
  }
}
